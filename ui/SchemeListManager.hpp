#ifndef SCHEMELISTMANAGER_HPP
#define SCHEMELISTMANAGER_HPP

#include <QObject>

class SchemeListManager : public QObject
{
    Q_OBJECT
public:
    explicit SchemeListManager(QObject *parent = 0);

signals:

public slots:

};

#endif // SCHEMELISTMANAGER_HPP
